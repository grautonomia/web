extends ./layout.jade

block body
    .row.homepage
        .large-12.columns
            .page
                .intro
                    h1= __('gra_full')
                    img.bird(src='/assets/bird.png')

                ul.articles.large-block-grid-2
                    - var articles = find({ locale: locale, type: 'article' }).sort({ date: -1 }).all()
                    each article, index in articles
                        li.article(class=index > 1 ? 'separator' : '' class=Math.floor(index/2) >= Math.floor(articles.length/2) ? 'last-row' : '')
                            h3: a(href='/'+article.path)= article.title
                            p.meta-data <i class="fa fa-fw fa-calendar"></i> #{prettyDate(article.date)} · <i class="fa fa-fw fa-clock-o"></i> #{__('x_minutes', article.readingTime)}
                            p!= article.snippet
                            //- a.more(href='/'+article.path) #{__('more')} &raquo;
